This is the backend server for ecoBay. Currently, it just
serves as a way to retrieve JSON-formatted data from the
eBay API's findItemsByKeyword functionality.

To run the server:
1. install Django: pip install django
2. python manage.py runserver